services:
    server:
        build:
            context: .
            dockerfile: server.Dockerfile
        environment:
            ENVIRONMENT: ${ENVIRONMENT:-PRODUCTION}
        ports:
            - ${PORT:-80}:8080
